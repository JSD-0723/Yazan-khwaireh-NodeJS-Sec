html 
  head 
    title Books
  
  body
    h1 Books
    if books.length == 0
      p There are no books.
    else
    ul 
      each book in books
   
        li The id of the book: #[span(style={color: "red"}) #{book.id}] and the name of the book: #[a(href="/books/" + book.id, style={color: "red"}) #{book.name}] 

    a(href="/add-book") Add a new book